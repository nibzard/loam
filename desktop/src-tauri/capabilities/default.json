{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the Loam desktop shell window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
